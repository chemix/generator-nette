parameters:

<% if (useDatabase){ %>
database:
	dsn: 'mysql:host=127.0.0.1;dbname=<%= database %>'
	user: '<%= username %>'
	password: '<%= password %>'
	options:
		lazy: yes
<% } else { %>
database:
	dsn: 'mysql:host=127.0.0.1;dbname=test'
	user:
	password:
<% } %>
